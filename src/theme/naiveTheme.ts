import type { GlobalThemeOverrides } from "naive-ui";

export const envaultThemeOverrides: GlobalThemeOverrides = {
  common: {
    primaryColor: "#D6712A",
    primaryColorHover: "#D8A250",
    primaryColorPressed: "#994525",
    primaryColorSuppl: "#D6712A",
    borderRadius: "12px",
  },
};
